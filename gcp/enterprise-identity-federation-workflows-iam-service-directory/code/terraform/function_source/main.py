logger.info(f"Received provisioning request for identity: [REDACTED]")
# Ensure the application does not bind to all network interfaces (0.0.0.0)
# For example, if using Flask:
# app.run(host="127.0.0.1")
